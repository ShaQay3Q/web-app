<h1>{{title}}</h1>
<nav>
  <ul>
    {{#each links}}
      <li>
        <a href="{{this.url}}" class="{{#if this.active}}active{{/if}}">
          {{this.name}}
        </a>
      </li>
    {{/each}}
  </ul>
</nav>
